<h1>Bienvenue sur votre boîte de reception</h1> 

<table class="table"> 

    <thead>
        <tr> 
            <td>id</td> 
            <td>Object</td> 
            <td>Body</td> 
            <td>Actions</td>
        </tr>

    </thead> 

    <tbody> 

    <% @email.each do |email| %>   
    
    <tr> 
    <td><%= email.id %></td>
    <td><%= email.object %></td> 
    <td> <%= link_to email.object, email_show_path(:id => email.id),  :remote => true %></td>
    <td> 
        <a href="<%= email_path(email.id) %>" class="btn btn-danger" data-confirm="sur?" data-method="delete">Supprimer</a> 
    </td> 


    </tr>
        <%end%>
    </tbody> 
</table>




<div class="link">
  <%= link_to "email", email_show_path, remote: true %>
</div>
<div id="show"></div>
